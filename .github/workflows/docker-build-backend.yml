name: Docker Build Backend

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: Build Backend Docker image and push to repositories
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Login to Docker Hub  
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    
    - name: Extract branch name
      shell: bash
      run: echo "branch=${GITHUB_HEAD_REF:-${GITHUB_REF#refs/heads/}}" >> $GITHUB_OUTPUT
      id: extract_branch

    - name: Build and push Backend Docker image
      uses: docker/build-push-action@v6
      with:
        context: .
        file: ./Dockerfile.backend
        push: true
        tags: |
          ${{ secrets.DOCKER_USERNAME }}/quotes-backend:latest
          ${{ secrets.DOCKER_USERNAME }}/quotes-backend:${{ github.sha }}
          ${{ secrets.DOCKER_USERNAME }}/quotes-backend:${{ steps.extract_branch.outputs.branch }}
        cache-from: type=gha
        cache-to: type=gha,mode=max